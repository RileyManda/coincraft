<div class="container-fluid card text-center d-flex flex-column justify-content-between" style="height: 95vh; max-width: 400px;">
<div class="app-bar" style="background-color: #3778c2; color: white; max-width: 100%;">
    <h2 class="text-center">Forgot your password?</h2>
    </div>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-group">
        <%= f.label :email %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
      </div>

      <div class="text-center">
        <%= f.submit "Send me reset password instructions", class: "btn btn-primary",style:"background-color: #3778c2;"  %>
      </div>
    <% end %>

    <div class="text-center">
      <%= render "devise/shared/links" %>
    </div>

</div>
